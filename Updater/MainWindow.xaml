<Window x:Class="Quotix.Updater.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quotix 更新"
        Width="520" Height="278"
        MinWidth="520" MinHeight="278"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="{StaticResource CardBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="46"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="Transparent"
                    BorderThickness="0"
                    CornerRadius="10,10,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal" Margin="18,0" VerticalAlignment="Center">
                    <TextBlock Text="Quotix 更新"
                               VerticalAlignment="Center"
                               Foreground="{StaticResource PrimaryTextBrush}"
                               FontSize="13" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>

            <Grid Grid.Row="1" Margin="28,22,28,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="正在更新"
                               Foreground="{StaticResource PrimaryTextBrush}"
                               FontSize="21" FontWeight="SemiBold"/>
                    <TextBlock x:Name="VersionText" Margin="0,5,0,0"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               FontSize="12.5"/>
                </StackPanel>

                <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="0,12,0,8">
                    <TextBlock x:Name="StatusText"
                               Text="等待中..."
                               TextWrapping="Wrap"
                               Foreground="{StaticResource PrimaryTextBrush}"
                               FontSize="14"/>
                    <ProgressBar x:Name="DownloadProgress"
                                 Height="6" Margin="0,14,0,9"
                                 Minimum="0" Maximum="100"
                                 Foreground="{StaticResource AccentBrush}"
                                 Background="#E4E7EA">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="3"
                                        SnapsToDevicePixels="True">
                                    <Grid x:Name="PART_Track" ClipToBounds="True">
                                        <Border x:Name="PART_Indicator"
                                                HorizontalAlignment="Left"
                                                Background="{TemplateBinding Foreground}"
                                                CornerRadius="3">
                                            <Border.RenderTransform>
                                                <TranslateTransform/>
                                            </Border.RenderTransform>
                                        </Border>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsIndeterminate" Value="True">
                                        <Setter TargetName="PART_Indicator" Property="Width" Value="88"/>
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation
                                                        Storyboard.TargetName="PART_Indicator"
                                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                                        From="-88" To="480"
                                                        Duration="0:0:1.35"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>
                    <Grid>
                        <TextBlock x:Name="SizeText"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   FontSize="12"/>
                        <TextBlock x:Name="SpeedText" HorizontalAlignment="Right"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   FontSize="12"/>
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="ReturnButton" Content="返回 Quotix"
                            Visibility="Collapsed"
                            Click="ReturnButton_Click" Margin="0,0,10,0"/>
                    <Button x:Name="RetryButton" Content="重新尝试"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Visibility="Collapsed"
                            Click="RetryButton_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
