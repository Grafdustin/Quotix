<Window x:Class="Quotix.Updater.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quotix 更新"
        Width="460" Height="296"
        MinWidth="460" MinHeight="296"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="{StaticResource CardBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="34"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="Transparent"
                    CornerRadius="10,10,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"/>

            <Grid Grid.Row="1" Margin="26,0,26,23">
                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <Image x:Name="AppIcon"
                           Source="pack://application:,,,/Resources/app.ico"
                           Width="52" Height="52"
                           Stretch="Uniform"
                           RenderOptions.BitmapScalingMode="HighQuality"/>

                    <TextBlock x:Name="StatusText"
                               Text="等待中..."
                               Margin="0,18,0,0"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               Foreground="{StaticResource PrimaryTextBrush}"
                               FontSize="18"
                               FontWeight="SemiBold"/>

                    <TextBlock x:Name="VersionText"
                               Margin="0,5,0,0"
                               HorizontalAlignment="Center"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               FontSize="11"/>

                    <TextBlock x:Name="FailureText"
                               Visibility="Collapsed"
                               Margin="0,7,0,0"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               FontSize="11"/>

                    <TextBlock x:Name="ProgressText"
                               Margin="0,18,0,6"
                               HorizontalAlignment="Center"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               FontSize="10"/>

                    <ProgressBar x:Name="DownloadProgress"
                                 Width="189" Height="5"
                                 Minimum="0" Maximum="100"
                                 Foreground="{StaticResource AccentBrush}"
                                 Background="#E4E7EA">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="2.5"
                                        SnapsToDevicePixels="True">
                                    <Grid x:Name="PART_Track" ClipToBounds="True">
                                        <Border x:Name="PART_Indicator"
                                                HorizontalAlignment="Left"
                                                Background="{TemplateBinding Foreground}"
                                                CornerRadius="2.5"/>
                                        <Border x:Name="IndeterminateIndicator"
                                                Width="44"
                                                HorizontalAlignment="Left"
                                                Background="{TemplateBinding Foreground}"
                                                CornerRadius="2.5"
                                                Visibility="Collapsed">
                                            <Border.RenderTransform>
                                                <TranslateTransform/>
                                            </Border.RenderTransform>
                                        </Border>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsIndeterminate" Value="True">
                                        <Setter TargetName="PART_Indicator" Property="Visibility" Value="Collapsed"/>
                                        <Setter TargetName="IndeterminateIndicator" Property="Visibility" Value="Visible"/>
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation
                                                        Storyboard.TargetName="IndeterminateIndicator"
                                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                                        From="-44" To="189"
                                                        Duration="0:0:1.3"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>

                    <TextBlock x:Name="SizeText" Visibility="Collapsed"/>
                    <TextBlock x:Name="SpeedText" Visibility="Collapsed"/>

                    <StackPanel x:Name="FailureActions"
                                Visibility="Collapsed"
                                Margin="0,18,0,0"
                                Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <Button x:Name="ReturnButton"
                                Content="取消"
                                Click="ReturnButton_Click"
                                Margin="0,0,8,0"/>
                        <Button x:Name="RetryButton"
                                Content="重试"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Click="RetryButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
