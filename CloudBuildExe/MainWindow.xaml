<Window x:Class="QuotixCloudBuild.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quotix 云端发布"
        Width="520"
        Height="620"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#FFF5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#0066CC" Padding="16,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="☁️" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="Quotix 云端发布" FontSize="15" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- 主内容 -->
        <ScrollViewer Grid.Row="1" Padding="16" Background="White">
            <StackPanel>

                <!-- 版本号 -->
                <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="版本号" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#333"/>
                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="当前版本：" VerticalAlignment="Center" Foreground="#666"/>
                            <TextBox Grid.Column="1" x:Name="CurrentVersionBox" IsReadOnly="True" Background="#F5F5F5"/>
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="新版本：" VerticalAlignment="Center" Foreground="#666"/>
                            <TextBox Grid.Column="1" x:Name="NewVersionBox"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 更新日志 -->
                <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="更新日志" FontWeight="SemiBold" Margin="0,0,0,4" Foreground="#333"/>
                        <TextBlock Text="以 # 开头的行作为章节标题" FontSize="11" Foreground="#999" Margin="0,0,0,6"/>
                        <TextBox x:Name="ChangelogBox"
                                 Height="140"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 FontFamily="Consolas"
                                 FontSize="12"/>
                    </StackPanel>
                </Border>

                <!-- 进度 -->
                <Border x:Name="ProgressBorder"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="12"
                        Margin="0,0,0,10"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="构建进度" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#333"/>
                        <ProgressBar x:Name="ProgressBar" Height="6" IsIndeterminate="True" Margin="0,0,0,6"/>
                        <TextBlock x:Name="ProgressText" FontSize="12" Foreground="#666"/>
                        <TextBlock x:Name="BuildStatusText" FontSize="12" Margin="0,4,0,0" Foreground="#0066CC"/>
                    </StackPanel>
                </Border>

                <!-- 日志 -->
                <Expander Header="详细日志" Margin="0,0,0,10">
                    <TextBox x:Name="LogBox"
                             Height="90"
                             IsReadOnly="True"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="11"
                             Background="#1E1E1E"
                             Foreground="#D4D4D4"/>
                </Expander>

            </StackPanel>
        </ScrollViewer>

        <!-- 底部按钮 -->
        <Border Grid.Row="2" Background="#F0F0F0" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="StartButton"
                        Content="开始发布"
                        Width="100" Height="32"
                        Background="#0066CC" Foreground="White"
                        BorderThickness="0"
                        Click="StartButton_Click"
                        Margin="0,0,8,0"/>
                <Button x:Name="CancelButton"
                        Content="取消"
                        Width="80" Height="32"
                        Background="White"
                        BorderBrush="#CCC"
                        IsEnabled="False"
                        Click="CancelButton_Click"/>
            </StackPanel>
        </Border>

    </Grid>

</Window>
